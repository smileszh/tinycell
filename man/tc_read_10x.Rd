% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_tc_read_10x.R
\name{tc_read_10x}
\alias{tc_read_10x}
\title{Read and Merge Single or Multiple 10X Genomics Datasets}
\usage{
tc_read_10x(
  data_dirs,
  project_name = "tinycell_project",
  min_cells = 3,
  min_features = 200
)
}
\arguments{
\item{data_dirs}{A single character string specifying the parent directory, OR a
named character vector of specific sample paths (e.g., \code{c(Ctrl = "./path1")}).}

\item{project_name}{Character string to set the project name of the merged Seurat
object. Default is \code{"tinycell_project"}.}

\item{min_cells}{Include features detected in at least this many cells. Default is 3.}

\item{min_features}{Include cells where at least this many features are detected. Default is 200.}
}
\value{
A merged \code{Seurat} object containing all processed samples. Cell names
are automatically prefixed with their respective sample names to prevent barcode collisions.
}
\description{
This function provides a streamlined workflow for reading 10X Genomics data and
creating a merged Seurat object. It supports two input modes:
\enumerate{
\item A single parent directory path: The function will automatically detect all
subdirectories containing 10X data and merge them.
\item A named character vector: The function will read the specific paths provided
and use the vector names as sample identifiers.
}
}
\examples{
\dontrun{
# Mode 1: Auto-detect all sample folders inside a parent directory
my_seurat <- tc_read_10x(data_dirs = "./data/raw_samples/")

# Mode 2: Specify paths manually using a named vector
paths <- c(Sample_A = "./data/SRR1", Sample_B = "./data/SRR2")
my_seurat <- tc_read_10x(data_dirs = paths, project_name = "My_Experiment")
}
}

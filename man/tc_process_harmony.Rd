% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_tc_process_harmony.R
\name{tc_process_harmony}
\alias{tc_process_harmony}
\title{Run Standard Integration and Dimensionality Reduction via Harmony}
\usage{
tc_process_harmony(
  object,
  batch_var = "orig.ident",
  dims = 1:30,
  resolution = 0.3,
  run_tsne = FALSE
)
}
\arguments{
\item{object}{A Seurat object.}

\item{batch_var}{Character. The metadata column to use for Harmony integration
(batch effect removal). Default is \code{"orig.ident"}.}

\item{dims}{Numeric vector. The PCA dimensions to use. Default is \code{1:30}.}

\item{resolution}{Numeric or numeric vector. Resolution parameter(s) for clustering.
If a vector is provided, it calculates all resolutions (useful for clustree).
Default is \code{0.3}.}

\item{run_tsne}{Logical. Whether to run tSNE alongside UMAP. Default is \code{FALSE}
to save computation time.}
}
\value{
A Seurat object with PCA, Harmony, UMAP (and optionally tSNE) reductions,
along with clustering results.
}
\description{
This function encapsulates the standard single-cell processing pipeline:
Normalization, Feature Selection, Scaling, PCA, Harmony batch correction,
Graph-based Clustering, and non-linear dimensionality reduction (UMAP/tSNE).
}
